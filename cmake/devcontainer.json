{
  "name": "devcontainer-cmake",
  "build": {
    "args": {
      "http_proxy": "${localEnv:http_proxy}",
      "https_proxy": "${localEnv:https_proxy}",
      "no_proxy": "${localEnv:no_proxy}",
      "CMAKE_VERSION": "v3.24.2",
      "NINJA_VERSION": "v1.11.1",
      "UBUNTU_IMAGE": "docker.io/library/ubuntu:jammy-20221003",
      "UBUNTU_MIRROR_PATH": "${localEnv:UBUNTU_MIRROR_PATH}"
    },
    "dockerfile": "Dockerfile"
  },
  "containerEnv": {
    "http_proxy": "${localEnv:http_proxy}",
    "https_proxy": "${localEnv:https_proxy}",
    "no_proxy": "${localEnv:no_proxy}"
  },
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "EditorConfig.EditorConfig",
        "ms-vscode.cmake-tools",
        "ms-vscode.cpptools",
        "ms-vscode.makefile-tools",
        "mwinters-stuff.ninjabuild",
        "VisualStudioExptTeam.vscodeintellicode"
      ]
    }
  },
  "mounts": [
    "source=devcontainer-cmake,target=/home/ubuntu/.vscode-server/extensions,type=volume"
  ],
  "remoteUser": "ubuntu"
}
